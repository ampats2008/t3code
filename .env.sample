# Enable Vite HMR (hot module replacement) for the web app dev server.
# Disabled by default so developing T3Code from within T3Code doesn't
# break the running client mid-edit. Set to 1 to re-enable.
# HMR=1
